# --- Disable Module Signing & Certificate Checks ---
# 彻底关闭内核模块签名机制
CONFIG_MODULE_SIG=n
CONFIG_MODULE_SIG_ALL=n
CONFIG_MODULE_SIG_FORCE=n

# 置空系统受信证书路径，避免编译时因缺少本地私钥/证书文件而中断
CONFIG_SYSTEM_TRUSTED_KEYS=""
CONFIG_SYSTEM_REVOCATION_KEYS=""
